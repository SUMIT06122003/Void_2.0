# TODO - Void Activewear Demo Bag + Variant Popup + Demo Checkout

- [x] Update `src/components/ProductGrid.jsx` to show variant-selection popup (color/size/pack) before adding to bag.
- [x] Persist selections into `voidCartDraft` as `selections`.
- [x] Update `src/pages/BagPage.jsx` to display saved variant selections for each bag row.
- [x] Implement demo checkout + demo payment flow in `src/pages/BagPage.jsx` (fake payment success, then clear bag).
- [x] Add/adjust CSS in `src/styles.css` for modal + checkout UI.
- [x] Run build/dev and manually test end-to-end flow.



